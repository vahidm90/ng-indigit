import { TCustomCharacter } from '../types';
export interface IPrettyFloatDecimalParam {
    floatPoint: TCustomCharacter;
    isDecimalAllowed: boolean;
    maxDigitCount: number;
    minDigitCount: number;
}
