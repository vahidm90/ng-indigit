import { IndigitDirective } from './indigit.directive';

describe('IndigitDirective', () => {
  it('should create an instance', () => {
    const directive = new IndigitDirective();
    expect(directive).toBeTruthy();
  });
});
